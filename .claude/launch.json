{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "aams-static-preview",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import http.server, os\nROOT = 'D:/Advanced Approval System'\nALLOWED = {'.html', '.css', '.js', '.png', '.svg', '.ico'}\nclass H(http.server.SimpleHTTPRequestHandler):\n    def __init__(self, *a, **k):\n        super().__init__(*a, directory=ROOT, **k)\n    def send_head(self):\n        p = self.path.split('?', 1)[0].split('#', 1)[0]\n        if p != '/' and os.path.splitext(p)[1].lower() not in ALLOWED:\n            self.send_error(404)\n            return None\n        return super().send_head()\nhttp.server.ThreadingHTTPServer(('127.0.0.1', 8765), H).serve_forever()\n"
      ],
      "port": 8765
    },
    {
      "name": "aams-app",
      "runtimeExecutable": "python",
      "runtimeArgs": ["main.py"],
      "port": 5000
    }
  ]
}
